export function resetTimer(timer) {
    
    clearInterval(timer);
    // document.querySelector('.circle').style.animation = '';
    // document.querySelector('.timecircle').classList.remove('circle');

}